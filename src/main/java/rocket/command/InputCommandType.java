package rocket.command;

/**
 * Stores all commands which can be expected from the user input.
 */
public enum InputCommandType {
    TODO, DEADLINE, EVENT, DELETE, EXIT, FIND, HELP, LIST, MARK, UNMARK, INVALID
}
